# JanusGraph configuration for HBase backend
storage.backend=hbase
storage.hostname=127.0.0.1
storage.port=2181
storage.hbase.table=janusgraph

# Cache settings
cache.db-cache = true
cache.db-cache-clean-wait = 20
cache.db-cache-time = 180000
cache.db-cache-size = 0.5

# Batch mutations before shipping to HBase
storage.buffer-size=2048
storage.write-time=100000

# Larger local ID blocks reduce ID-authority round trips
ids.block-size=100000
ids.renew-percentage=0.3

# After compose down the previous JVM is gone but its instance id stays in the graph.
# Replace that stale lock so the new container can open the same table.
graph.replace-instance-if-exists=true

# Setup server to accept Gremlin connections
gremlin.graph=org.janusgraph.core.JanusGraphFactory
storage.setup-wait=300000
storage.hbase.ext.hbase.rpc.timeout=300000
storage.hbase.ext.hbase.client.operation.timeout=300000
ids.authority.wait-time = 5000
ids.authority.random-bound = 0
storage.hbase.ext.hbase.client.durability=ASYNC_WAL
storage.hbase.ext.hbase.client.write.buffer=8388608
