# Stream I/O design (internal)

Pointer file so `{@code STREAM_IO_DESIGN.md}` JavaDoc and test references
resolve when browsing this module.

Chunked stream I/O for SecureCloud. The full internal specification is
not published with this source tree.
