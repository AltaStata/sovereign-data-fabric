# Sharing design (internal)

Pointer file so `{@code SHARING_DESIGN.md}` references resolve when
browsing this module.

S3-gateway sharing semantics. The full internal specification is not
published with this source tree.
