# gRPC plan (internal)

Pointer file so `{@code GRPC_PLAN.md}` references resolve when browsing
this module.

`altastata-grpc` is a library consumed by the Services uber JAR. The full
internal plan is not published with this source tree.
