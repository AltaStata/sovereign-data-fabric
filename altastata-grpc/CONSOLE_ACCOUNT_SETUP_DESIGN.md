# Console account-setup design (internal)

Pointer file so `{@code CONSOLE_ACCOUNT_SETUP_DESIGN.md}` JavaDoc
references resolve when browsing this module.

Covers `GenerateKeys`, `LoginV2`, and `IssueCredentials` (the path that
replaces admin HTTP PUTs). The full internal specification is not
published with this source tree.
