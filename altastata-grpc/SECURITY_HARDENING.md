# Security hardening (internal)

Pointer file so `{@code SECURITY_HARDENING.md}` references resolve when
browsing this module.

Future hardening notes (bind-address policy, session caps, Console CSP).
The full internal specification is not published with this source tree.
