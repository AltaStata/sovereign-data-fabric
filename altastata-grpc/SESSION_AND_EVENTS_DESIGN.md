# Session and events design (internal)

Pointer file so `{@code SESSION_AND_EVENTS_DESIGN.md}` JavaDoc references
resolve when browsing this module.

Covers `sess-<token>` session lifecycle, `SessionRegistry`, and the
per-session `EventBus` / `Watch` fan-out. The full internal specification
is not published with this source tree.
