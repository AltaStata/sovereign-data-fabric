# Upload design (internal)

Pointer file so `{@code UPLOAD_DESIGN.md}` references resolve when
browsing this module.

Future gRPC upload notes. The full internal specification is not
published with this source tree.
